import styled from 'styled-components';
import { colors } from '../../../Theme';

export const SAttention = styled.h3`
  font-size: 16px;
  font-weight: 700;
  color: ${colors.brightOrange};
  position: absolute;
  bottom: 9px;
  right: 17px;
`;
